import PortfolioSection from '@/components/sections/PortfolioSection';
import Layout from '@/components/Layout';

export default function ClientsPage() {
  return (
    <Layout pageTitle="Clients" pageClass="clients-page">
      <PortfolioSection />
    </Layout>
  );
}
